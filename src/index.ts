// class Throttle {
//   constructor (fn, timer, max) {
//   }
//   interval() {
//   }
// }

// Parameter fn should not use "this", for simplicity.
function throttle() {
  return true;
}

export default throttle;
